# 本地开发时无需改动（默认已指向 http://localhost:8000）
# 部署到 Vercel 后，在 Vercel 项目的 Environment Variables 里新增同名变量，
# 值改成你 Render 后端的线上地址，例如：
# VITE_API_BASE=https://tanci-ocr-backend.onrender.com
VITE_API_BASE=http://localhost:8000
